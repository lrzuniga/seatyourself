<h5> New Reservation </h5>

<%= form_for ([@restaurant, @reservation]) do |f| %>

<%= f.label :party_size %>
<%= f.number_field :party_size %>

<%= f.label :time %>
<%= f.datetime_select :time, :discard_year => true %>


<%= f.submit %>

<% end %>