
	<h3> <%= @restaurant.name %> </h3>
	<ul>
		<li> Address: <%= @restaurant.address %> </li>
		<li> <%= @restaurant.description %> </li>
		<li> Available Seats: <%= @restaurant.seats %> </li>
	</ul>
<p><%= link_to "Edit Restaurant", edit_restaurant_path(@restaurant) %> || 
<%= link_to "Delete Restaurant", @restaurant, :confirm => "Are you sure you want to delete this restaurant?", :method => :delete %> </p>
